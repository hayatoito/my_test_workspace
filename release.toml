no-dev-version = true
# dev-version-ext = "pre"
pre-release-commit-message  = "Prepare to {{crate_name}}-v{{version}}"
post-release-commit-message = "Start next development iteration {{crate_name}}-v{{next_version}}"
# tag-message                 = "Bump version to v{{version}}"
# tag-prefix                  = ""
